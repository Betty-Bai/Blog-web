<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Web</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="./css/style.css" rel="stylesheet">
</head>
<body>
    <div class="px-4 py-5 my-5 text-center">
        
        <h1 class="display-5"><img class="mx-auto mb-4" src="../images/blog.png" alt="" height="57"> Blog Web</h1>
        <form action="/newpost" method="post">
            <div class="col-lg-6 mx-auto">
                <div class="input-group mb-3">
                    <span class="input-group-text">Author</span>
                    <input name="author" type="text" class="form-control" placeholder="Author">
                    <span class="input-group-text">Subject</span>
                    <input name="subject" type="text" class="form-control" placeholder="Subject">
                </div>
                <div class="input-group mb-3">
                    <span class="input-group-text">Content</span>
                    <textarea name="post" class="form-control"></textarea>
                </div>
            <div class="d-grid d-sm-flex justify-content-sm-center">
                <button type="submit" class="btn btn-primary btn-lg px-4 gap-3">New Post</button>
            </div>
            </div>
        </form>
      </div>

    <h1> Jing's Blog Web</h1>
    <form action="/newpost" method="post">
        <input name="subject" type="text" class="feedback-input" placeholder="Subject" />
        <textarea name="post" class="feedback-input" placeholder="Post"></textarea>
        <input type="submit" value="POST" />
    </form>
    <% if (locals.poste) {%>
        <% for (var i = poste.length-1; i >-1; i--) { %> 
            <h2><%= poste[i].subject %></h2>
            <p><%= poste[i].post %></p>
            <small>Author: <%= poste[i].author %></small><br>
            <small><%= poste[i].date %></small><br>
            <a class="edit" href="/edit/<%= poste[i].id %>">Edit</a>
            <a class="delete" href="/delete/<%= poste[i].id %>">Delete</a>
        <% } %>
    <% } %> 
</body>
</html> 